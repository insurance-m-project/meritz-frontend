name: deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2

      - run: touch .env
      - run: echo "${{ secrets.ENV_FILE }}" > .env
      - run: cat .env


      - name: Docker build & push to dev
        run: |
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker build -t ${{ secrets.DOCKER_USERNAME }}/hospital-web .
          docker push ${{ secrets.DOCKER_USERNAME }}/hospital-web

      - name: Deploy to dev
        uses: appleboy/ssh-action@master
        id: deploy-dev
        with:
          host: ${{ secrets.HOST_DEV }} # GCP 퍼블릭 IPv4 DNS
          username: ${{ secrets.USERNAME }} # GCP NAME
          port: 22
          key: ${{ secrets.PRIVATE_KEY }}
          script: |
            docker rm -f $(docker ps --filter name=hospital -a -q)
            docker pull ${{ secrets.DOCKER_USERNAME }}/hospital-web
            docker run -d -p 4000:4000 ${{ secrets.DOCKER_USERNAME }}/hospital-web
            docker image prune -f
